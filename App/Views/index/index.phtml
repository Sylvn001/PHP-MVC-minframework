<h2>Home </h2>

<?php 

    foreach($this->view->data as $key => $product){
        $prod = 
            $product['id'] . '-' . 
            $product['description'] . '-' . 
            $product['price'];
        echo $prod . '<br>';
    }
