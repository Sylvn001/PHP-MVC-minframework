<h2 class="text-primary">About </h2>

<?php 
    foreach($this->view->data as $key => $info){
        $inf =   $info['id'] . '-'.
        $info['title'] . '-' .
        $info['description'] . '-';

        echo $inf . '<br>';
    }
